<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerMovie"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="none">

    </androidx.recyclerview.widget.RecyclerView>

    <ProgressBar
        android:id="@+id/progressBarSimilar"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:outlineAmbientShadowColor="@color/colorAccent"
        app:layout_constraintBottom_toBottomOf="@+id/recyclerMovie"
        app:layout_constraintEnd_toEndOf="@+id/recyclerMovie"
        app:layout_constraintStart_toStartOf="@+id/recyclerMovie"
        app:layout_constraintTop_toTopOf="@+id/recyclerMovie"  />

</androidx.constraintlayout.widget.ConstraintLayout>